# Copie este arquivo para application-local.yml (mesma pasta)
# application-local.yml está no .gitignore — NÃO commite senhas
#
# O perfil "dev" carrega automaticamente "local" junto (ver application.yml)

spring:
  datasource:
    url: jdbc:mysql://SEU-HOST.tidbcloud.com:4000/projeto_mta?sslMode=VERIFY_IDENTITY&serverTimezone=UTC&allowPublicKeyRetrieval=true&connectTimeout=10000&socketTimeout=30000
    username: SEU_USUARIO.root
    password: SUA_SENHA
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  seed:
    admin-email: admin@mta.org
    admin-password: SUA_SENHA_ADMIN
