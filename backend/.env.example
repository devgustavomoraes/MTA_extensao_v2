# Copie para backend/.env e ajuste se necessário (NÃO commite o .env real)

SPRING_PROFILES_ACTIVE=dev

# Segurança
JWT_SECRET=substitua-por-string-aleatoria-com-pelo-menos-32-caracteres
JWT_EXPIRATION_MS=86400000

# CORS (URL do front Vite)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Contas admin: backend/src/main/resources/application-admins.yml (gitignored)
